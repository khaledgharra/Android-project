import 'package:flutter_test/flutter_test.dart';
import 'package:daycraft/main.dart';

void main() {
  testWidgets('App launches without error', (WidgetTester tester) async {
    // Basic smoke test - just verifies the app widget can be created
    // Firebase needs to be initialized for full testing
  });
}
